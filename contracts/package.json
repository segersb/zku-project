{
  "name": "zku-project",
  "version": "1.0.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "hardhat-node": "npx hardhat node",
    "deploy:local:events": "hardhat deploy:events --network localhost",
    "deploy:local:polls": "hardhat deploy:polls --network localhost",
    "deploy:local": "yarn deploy:local:events && yarn deploy:local:polls",
    "deploy:goerli:events": "hardhat deploy:events --network goerli",
    "deploy:goerli:polls": "hardhat deploy:polls --network goerli"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.1.0",
    "@nomiclabs/hardhat-etherscan": "^3.1.0",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "@openzeppelin/contracts": "^4.7.3",
    "@zk-kit/incremental-merkle-tree": "^0.4.3",
    "chai": "^4.3.6",
    "circomlibjs": "^0.1.7",
    "dotenv": "^16.0.1",
    "ethereum-waffle": "^3.4.4",
    "ethers": "^5.6.9",
    "hardhat": "^2.10.1",
    "hardhat-gas-reporter": "^1.0.8",
    "snarkjs": "^0.4.24"
  }
}
